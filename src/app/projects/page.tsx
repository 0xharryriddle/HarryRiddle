import React from "react";

// Components
import Repositories from "@/components/projects/Respositories";

const Projects = () => {
    return (
        <main>
            <Repositories />
        </main>
    );
};

export default Projects;
